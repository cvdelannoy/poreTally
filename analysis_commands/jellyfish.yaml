versions:
  jellyfish: 'jellyfish --version | grep -Po "(?<=jellyfish ).+"'
commands:
  jellyfish count -m 5 -s 100M -t {{threads}} -C {{input}} -o tmp.jf
  jellyfish dump -c tmp.jf > {{output}}
conda:
  channels:
  - bioconda
  dependencies:
  - jellyfish